{# This file demonstrates using most of the index.twig template and modifying
just a small part. See `search.twig` for an example of another approach #}

{% extends "index.twig" %}

{% block content %}

	{# Showing posts archive #}
	{% if posts|length > 0 %}

		{% if post_cat[0].cat_name == title %}
			<section class="firstclass-category-content-left w-full mx-auto {% if fields.pagepicker.light_verstion %}light-version{% endif %}">
				<div class="container mx-auto py-[80px] grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
					{% for post in posts %}
					{% set category_id = fields.blog.term_id %}
					{% set link = fn('get_permalink', post.ID) %}
					{% set thumbnail_url = fn('get_the_post_thumbnail_url', post.ID ) %}
			
        <article class="h-full pr-[1px]">

          <div class="border border-[#4E4D4D29] rounded-[8px] p-[16px] pb-0"> 
          
            <img src="{{ thumbnail_url }}"
                 class="h-[210px] w-full object-cover rounded-[6px] mb-[30px]">
            <div class="flex flex-col flex-grow pb-[24px] md:px-[36px] md:pb-[36px]">

              <div class="flex items-center uppercase gap-[8px] mb-[25px]">
                <img src="{{ site.theme.link }}/images/icons/calendar.svg">
                <span class="text-[12px]">{{ post.published_at|date("j F Y") }}</span>
              </div>

              <h3 class="text-[18px] font-bold leading-[24px] mb-[25px]">
                {{ post.post_title }}
              </h3>

              <p class="mb-[30px] flex-grow">
               {{ post.post_excerpt|split(' ')|slice(0, 20)|join(' ') ~ '...' }}
              </p>

              <a href="{{ link }}"
                 class="flex items-center justify-center w-[131px] h-[48px] rounded-[5px] bg-blue text-white text-[16px] font-bold text-center">
                Read More
              </a>

            </div>
          
          </div>
        </article>
					{% endfor %}
				</div>
			</section>


		{% endif %}
	{% elseif posts|length == 0 and categories == 0 %}
		<div class="section px-10 py-[80px] mb-10">
			<h2 class="text-4xl font-light mb-4">Unfortunately no match was found.... try again!</h2>
		</div>
	{% endif %}


{% endblock %}
